snapshots:
    - name: dim_stores
      relation: ref('eph_stores')
      config:
        schema: gold
        database: wallmart
        unique_key: store_id
        strategy: timestamp
        updated_at: store_updated_timestamp
        dbt_valid_to_current: "to_date('9999-12-31')"

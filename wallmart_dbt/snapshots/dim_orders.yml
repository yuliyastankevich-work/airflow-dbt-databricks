snapshots:
    - name: dim_orders
      relation: ref('eph_orders')
      config:
        schema: gold
        database: wallmart
        unique_key: order_id
        strategy: timestamp
        updated_at: order_updated_timestamp
        dbt_valid_to_current: "to_date('9999-12-31')"
